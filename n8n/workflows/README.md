# n8n workflows

Commit exported, credential-free workflow JSON here. Staging is the authoring
environment; production only receives reviewed exports from `main`.

`roof-lookup-workflow.json` is intentionally not fabricated here: it was named
in the engineering brief but was not supplied with the brief or found in this
repository. Add the original export here before the staging import/test step.
